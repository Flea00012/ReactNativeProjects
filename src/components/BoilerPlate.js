import React from "react";
import { View, Text, StyleSheet } from "react-native";

const ImageDetails = () => {
  return (
    <View>
      <Text> ImageDetails list </Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default ImageDetails;
